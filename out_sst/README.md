# Corrected SST time series

This directory contains the bias-corrected SST time series generated for each model, region, experiment, and correction method.

These files are outputs from the bias-removal workflow implemented in `scripts/bias_removal.py`.
